---
import { getStatusBadge } from "../lib/badges";

interface Props {
  status: string;
}

const { status } = Astro.props;
const badge = getStatusBadge(status);
---

<span class:list={["badge", "status-badge", badge.className]}>
  <span class="sr-only">Status:</span>
  {badge.label}
</span>
